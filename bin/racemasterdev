#!/usr/bin/env node

'use strict';

require('babel-register');
require('../lib/setup');
/*
var Commands = require('../lib/commands').Commands;
var commands = new Commands(process.argv);
commands.execute(process.argv);
*/
