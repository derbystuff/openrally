const React = require('react');

module.exports = React.createClass({
  render(){
    return (
      <div>
        <h1>About</h1>
        <p>RaceMaster is a race management system focused on simplifing running
        races for AA and NDR race cities.</p>
      </div>
    );
  }
});
